#include "main.h"
/**
 *      add - Entry Point
 *  Description: add fucntion add 2 integers and return result.
 *	@a : first integers.
 *	@b : secoend Integer.
 *	Return: result of Sum a & b.
 *      by a.shazally.
**/
int add(int a, int b)
{
int result;

result = a + b;
return (result);
}
