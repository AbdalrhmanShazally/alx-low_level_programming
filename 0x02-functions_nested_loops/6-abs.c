#include "main.h"
/**
 *	_abs - Entry Point
 *  Description: _abs fucntion return number absolute
 *      @n: is the number that will be absolute
 *      Return: absolute number
 *      by a.shazally.
**/
int _abs(int n)
{
	if (n >= 0)
	{
		return (n);
	}
	return (-n);
}
